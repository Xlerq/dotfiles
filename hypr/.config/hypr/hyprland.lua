-- Hyprland Lua config.
-- Legacy .conf files are kept as a fallback while Hyprland still supports them.

require("lua.env")
require("lua.monitor")
require("lua.look_and_feel")
require("lua.rules")
require("lua.keys")
require("lua.start_programs")
